# Configuración del servidor
PORT=3000
NODE_ENV=development

# Configuración de MySQL
DB_HOST=localhost
DB_USER=evaluate
DB_PASSWORD=evaluate123
DB_NAME=evaluate


# Notas:
# 1. Copia este archivo a .env y ajusta los valores según tu configuración
# 2. Asegúrate de crear el usuario MySQL con los permisos necesarios:
#    CREATE USER 'evaluate'@'localhost' IDENTIFIED BY 'evaluate123';
#    GRANT ALL PRIVILEGES ON evaluate.* TO 'evaluate'@'localhost';
#    FLUSH PRIVILEGES;
